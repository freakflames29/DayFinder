# frozen_string_literal: true

RSpec.describe DayFinder do
  it "has a version number" do
    expect(DayFinder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
